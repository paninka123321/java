<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Pasek baterii</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>

  <!-- pojedynczy poziomy pasek baterii -->
  <div class="battery-bar">
    <div class="battery-info">
      <span class="battery-icon">📱</span>
      <span class="battery-label">This phone</span>
    </div>

    <div class="battery-progress">
      <div class="battery-fill" id="batteryFill"></div>
    </div>

    <div class="battery-percent" id="batteryPercent">90%</div>
  </div>

  <script src="script.js"></script>
</body>
</html>

/* wygląd całego paska */
.battery-bar {
  width: 250px;
  background: #fff; /* białe tło */
  border-radius: 12px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.1);
  padding: 10px 12px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  font-family: sans-serif;
  color: #000; /* czarny tekst i ikonka */
}

/* sekcja z ikoną i nazwą */
.battery-info {
  display: flex;
  align-items: center;
  gap: 6px;
}

.battery-icon {
  font-size: 18px;
}

.battery-label {
  font-size: 14px;
  font-weight: 500;
}

/* kontener paska postępu */
.battery-progress {
  flex-grow: 1;
  height: 10px;
  background: #ddd;
  border-radius: 6px;
  margin: 0 10px;
  overflow: hidden;
  position: relative;
}

/* kolorowy pasek */
.battery-fill {
  height: 100%;
  width: 0%;
  background: #4cd964; /* zielony kolor jak w iOS */
  border-radius: 6px;
  transition: width 0.4s ease-in-out; /* płynna animacja */
}

/* procent po prawej stronie */
.battery-percent {
  font-size: 14px;
  font-weight: 600;
}

const fill = document.getElementById("batteryFill");
const percentText = document.getElementById("batteryPercent");

/**
 * Funkcja aktualizująca pasek
 * @param {number} value - poziom procentowy baterii (0–100)
 */
function updateBar(value) {
  const percent = Math.min(Math.max(value, 0), 100); // ograniczenie do zakresu
  fill.style.width = percent + "%";
  percentText.textContent = percent + "%";
}

// przykład dynamicznej zmiany co 2 sekundy (można usunąć)
let demo = 90;
setInterval(() => {
  demo = (demo + 10) % 110;
  updateBar(demo);
}, 2000);

// początkowe ustawienie
updateBar(90);
