const canvas = document.getElementById("batteryCanvas");
const ctx = canvas.getContext("2d");
const percentText = document.getElementById("batteryPercent");

// skalowanie pod urządzenie
const scale = window.devicePixelRatio || 1;
canvas.width = 120 * scale;  // dopasuj do swojego rozmiaru
canvas.height = 120 * scale;
ctx.scale(scale, scale);

let batteryValue = 75;
const color = "#4cd964";

function drawBatteryGauge(value) {
  const size = 120; // rozmiar widoczny (bez skalowania)
  const center = size / 2;
  const radius = center - 8;
  const startAngle = 0;
  const endAngle = 2 * Math.PI;

  // czyść całość (uwzględnij skalowanie)
  ctx.clearRect(0, 0, size, size);

  // tło
  ctx.beginPath();
  ctx.arc(center, center, radius, startAngle, endAngle);
  ctx.strokeStyle = "#3a3a3c";
  ctx.lineWidth = 10;
  ctx.stroke();

  // pasek postępu
  const fraction = Math.min(Math.max(value / 100, 0), 1);
  const progressAngle = startAngle + fraction * endAngle;

  ctx.beginPath();
  ctx.arc(center, center, radius, startAngle, progressAngle);
  ctx.strokeStyle = color;
  ctx.lineWidth = 10;
  ctx.lineCap = "round";
  ctx.stroke();

  percentText.textContent = `${Math.round(value)}%`;
}

function updateGauge(newValue) {
  batteryValue = newValue;
  drawBatteryGauge(batteryValue);
}

drawBatteryGauge(batteryValue);

let demoVal = 75;
setInterval(() => {
  demoVal = (demoVal + 5) % 101;
  updateGauge(demoVal);
}, 2000);
